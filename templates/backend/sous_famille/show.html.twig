{% extends 'base.html.twig' %}

{% block title %}SousFamille{% endblock %}

{% block body %}
    <h1>SousFamille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_famille.id }}</td>
            </tr>
            <tr>
                <th>CodeSF</th>
                <td>{{ sous_famille.codeSF }}</td>
            </tr>
            <tr>
                <th>LibelleSF</th>
                <td>{{ sous_famille.libelleSF }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_famille_index') }}">back to list</a>

    <a href="{{ path('app_sous_famille_edit', {'id': sous_famille.id}) }}">edit</a>

    {{ include('sous_famille/_delete_form.html.twig') }}
{% endblock %}
